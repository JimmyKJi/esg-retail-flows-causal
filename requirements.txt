# ── Core data ──────────────────────────────────────────────
pandas>=2.1
numpy>=1.26
scipy>=1.11
polars>=0.20          # fast 13F × institution joins
pyarrow>=15           # parquet engine for the analysis panel

# ── Causal / econometrics ──────────────────────────────────
pyfixest>=0.18        # event study + Sun–Abraham interaction-weighted estimator
differences>=0.2      # Callaway–Sant'Anna staggered-adoption DiD
linearmodels>=6.0     # panel FE / IV cross-checks
statsmodels>=0.14     # structural-break (Chow / Quandt–Andrews) + misc
scikit-learn>=1.3     # propensity-score / coarsened-exact matching

# ── Data acquisition ───────────────────────────────────────
requests>=2.31        # SEC EDGAR + iShares pulls (declared User-Agent, rate-limited)
yfinance>=0.2.40      # prices / returns
# Fama–French factors are pulled by direct download in src/ingest/ff_factors.py
# (pandas-datareader is intentionally omitted: it is incompatible with pandas 3.x).
lxml>=5.0             # N-PORT XML + HTML table parsing
beautifulsoup4>=4.12  # Wikipedia S&P 500 change-list parsing

# ── Viz ────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13

# ── Tooling ────────────────────────────────────────────────
pytest>=8.0
tqdm>=4.66
python-dotenv>=1.0
jupyter>=1.0
ipykernel>=6.27
